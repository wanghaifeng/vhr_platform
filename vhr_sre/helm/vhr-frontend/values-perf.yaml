# Production environment values
replicaCount: 2

image:
  tag: "perf"

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 200m
    memory: 256Mi

ingress:
  hosts:
    - host: vhr-perf.example.com
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: vhr-frontend-perf-tls
      hosts:
        - vhr-perf.example.com

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 5
